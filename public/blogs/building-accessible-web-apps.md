An accessible web is a better web for everyone.

## Semantic HTML

Always prefer native HTML elements over `<div>` soups. A `<button>` comes with keyboard navigation and screen reader support out of the box.

## Alt Text

Never neglect the `alt` attribute on images. It is vital for users who rely on screen readers.

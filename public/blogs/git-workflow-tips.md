Just because nobody else is reviewing your code doesn't mean your commit history should be a mess.

## Meaningful Commits

Commit often, but commit logical chunks of work. "Fixed bug" is a terrible commit message. "Fix: Resolves overflow issue in navbar on mobile" is much better.

## Feature Branches

Always branch off main for a new feature. It keeps your experiments isolated.
